# SSH Remote Tool 使用说明书

> 版本：v0.1.0  
> 作者：fan JC  
> 邮箱：1071295829@qq.com  
> 更新日期：2026-04-30

---

## 目录

1. [软件简介](#1-软件简介)
2. [安装与启动](#2-安装与启动)
3. [主界面概览](#3-主界面概览)
4. [主机管理页](#4-主机管理页)
5. [任务编排页](#5-任务编排页)
6. [执行监控页](#6-执行监控页)
7. [关于对话框](#7-关于对话框)
8. [常见问题](#8-常见问题)

---

## 1. 软件简介

**SSH Remote Tool** 是一款基于 Python 开发的 SSH 远程运维工具，适用于需要批量管理多台远程主机、执行命令和传输文件的运维场景。

**核心功能：**
- 🖥 **批量主机管理** — 统一管理多台 SSH 主机，支持密码/密钥认证
- 📋 **可视化任务编排** — 拖拽式步骤编排，支持命令执行和文件传输
- 📊 **实时执行监控** — 多主机并行执行，实时查看日志和进度
- 📁 **CSV 批量导入导出** — 支持从 CSV 文件批量导入主机信息

**技术架构：** PySide6（界面） + paramiko（SSH/SFTP） + SQLAlchemy（数据持久化） + SQLite（本地数据库）

---

## 2. 安装与启动

### 2.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 7 及以上 |
| Python | 3.8 及以上（推荐使用安装包自带环境） |
| 依赖库 | PySide6、paramiko、SQLAlchemy、cryptography |

### 2.2 首次启动

**方式一：直接运行 Python 脚本**
```bash
cd ssh-remote-tool
python main.py
```

**方式二：使用打包后的可执行文件**

运行 `dist/SSHRemoteTool/SSHRemoteTool.exe`，或通过 Inno Setup 安装包安装后从桌面快捷方式启动。

首次启动时，软件会自动在以下目录创建数据文件：

| 文件 | 路径 |
|------|------|
| 数据库 | `C:\Users\你的用户名\.ssh-remote-tool\ssh_manager.db` |
| 日志目录 | `C:\Users\你的用户名\.ssh-remote-tool\logs\` |

> 💡 可通过环境变量 `SRT_DB_DIR` 自定义数据目录位置。

---

## 3. 主界面概览

启动后，主界面为白色亮色主题，包含以下区域：

```
┌──────────────────────────────────────────────────┐
│  文件    工具    帮助          ← 菜单栏          │
├──────────────────────────────────────────────────┤
│  🖥 主机管理  │  📋 任务编排  │  📊 执行监控  │ ← 页面标签
├──────────────────────────────────────────────────┤
│                                                  │
│           当前页面的主要内容区域                    │
│                                                  │
├──────────────────────────────────────────────────┤
│  📋 任务名称：xxx    总耗时： 00:00  │  就绪   │ ← 状态栏
└──────────────────────────────────────────────────┘
```

**三个主页面：**
- **🖥 主机管理** — 管理 SSH 主机信息
- **📋 任务编排** — 创建和编辑执行任务
- **📊 执行监控** — 执行任务并查看实时日志

**菜单栏说明：**
- `文件` → `退出`：关闭软件
- `工具` → `设置`：（开发中）
- `帮助` → `关于`：查看软件版本和作者信息

---

## 4. 主机管理页

主机管理页用于管理所有 SSH 远程主机，支持增删改查、连接测试和批量导入导出。

### 4.1 界面布局

```
┌──────────────────────────────────────────────────────┐
│  [+ 添加主机]  [全选] [反选]  搜索框：[________] │ ← 操作栏
├────┬────────┬──────────┬──────┬────────┬──────┬──────┤
│ 状态│ 主机名 │ IP:端口   │用户名│ 认证方式│ 分组 │ 操作 │ ← 表头
├────┼────────┼──────────┼──────┼────────┼──────┼──────┤
│ ◆  │ web01  │ 10.0.0.1│ root │ 密码   │ Web  │ 按钮 │
│ ●  │ db01   │ 10.0.0.2│ admin│ 密钥   │ DB   │ 按钮 │
└────┴────────┴──────────┴──────┴────────┴──────┴──────┘
```

**主机表格列说明：**

| 列 | 说明 |
|----|------|
| 状态 | ● 绿色=在线，● 红色=离线，● 灰色=未测试，● 黄色=测试中 |
| 主机名 | 默认与 IP 相同，可自定义 |
| IP:端口 | SSH 连接地址和端口（默认 22） |
| 用户名 | SSH 登录用户名 |
| 认证方式 | 显示"密码"或"密钥: 路径"，可快速切换 |
| 分组 | 主机分组标签，便于分类管理 |
| 备注 | 备注信息 |
| 操作 | 编辑和测试连接两个按钮 |

### 4.2 添加主机

点击 `添加主机` 按钮，弹窗填写主机信息：

- **主机名**：自定义名称（留空则自动使用 IP 作为主机名）
- **主机地址**：IP 或域名
- **端口**：SSH 端口，默认 22
- **用户名**：登录用户名
- **认证方式**：选择"密码"或"密钥文件"
  - 密码方式：输入密码（保存到数据库时自动加密存储）
  - 密钥方式：点击 `浏览` 按钮选择私钥文件
- **分组**：输入分组名称，便于批量筛选
- **备注**：可选备注信息

填写完成后点击 `保存`。

### 4.3 行内编辑

表格支持**双击直接进入编辑模式**，无需打开弹窗：

1. 双击任意单元格（操作列除外）
2. 表格进入编辑状态，相关字段变为可编辑控件
3. 编辑完成后按 `Enter` 自动保存并跳转下一字段
4. 按 `Esc` 取消编辑
5. 点击其他区域也会自动保存

**行内编辑支持修改：** 主机名、IP/端口、用户名、认证方式（含密码/密钥切换）、分组、备注。

### 4.4 测试连接

有两种方式测试主机连接：

- **单个测试**：点击主机所在行的 `测试` 按钮
- **批量测试**：勾选多台主机，点击工具栏的搜索栏旁边的功能区域批量操作

测试结果显示在状态列：
- ● 绿色 — 连接成功
- ● 红色 — 连接失败（将鼠标悬浮在状态图标上查看错误原因）

### 4.5 批量导入导出

#### 导出主机

点击工具栏的导出按钮，选择保存位置，导出为 CSV 格式（兼容 Excel）。

导出文件包含以下字段：

| 字段 | 说明 |
|------|------|
| 主机名 | 主机名称 |
| IP地址 | 主机 IP 或域名 |
| 端口 | SSH 端口 |
| 用户名 | 登录用户名 |
| 认证方式 | "密码" 或 "密钥" |
| 密码 | 导出时显示为 `********`（不会泄露真实密码）|
| 密钥路径 | 私钥文件路径 |
| 分组 | 主机分组 |
| 备注 | 备注信息 |

> ⚠️ 密码在导出文件中显示为 `********`，重新导入时该占位符会被忽略，保留数据库中原有密码不变。

#### 导入主机

点击工具栏的导入按钮，选择 CSV 或 JSON 文件。

- 支持 UTF-8 和 GBK 编码自动识别
- 主机名已存在时**更新**该主机信息（而非跳过）
- 密码列为 `********` 时，保留原密码不变
- 其他字段均会被更新

### 4.6 搜索与筛选

- **搜索框**：输入关键词，实时过滤主机列表（匹配主机名、IP、分组、备注）
- **全选/反选按钮**：快速选中/取消选中当前过滤后的所有主机
- 搜索状态下进行编辑/删除操作，只会影响已选中的主机

### 4.7 删除主机

点击主机所在行的 `删除` 按钮，确认后删除该主机。

---

## 5. 任务编排页

任务编排页用于创建和管理执行任务，每个任务由多个有序步骤组成。

### 5.1 界面布局

```
┌──────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────────────────────┐ │
│  │  已保存任务   │  │  执行步骤                     │ │
│  │  ● 任务A     │  │  ┌──────────────────────────┐│ │
│  │  ● 任务B     │  │  │ 步骤1: 检查磁盘空间      ││ │
│  │              │  │  │ [编辑][复制][模板][上移]... ││ │
│  │ [+ 新建任务]  │  │  └──────────────────────────┘│ │
│  └──────────────┘  │  ┌──────────────────────────┐│ │
│                    │  │ 步骤2: 备份配置文件       ││ │
│                    │  │ ...                       ││ │
│                    │  └──────────────────────────┘│ │
│                    │  [+ 添加步骤] [从模板添加]    │ │
│                    └──────────────────────────────┘ │
│  ┌────────────────────────────────────────────────┐ │
│  │  >> 目标主机  │ 搜索：[________] [全选][反选] │ │
│  │  ☑ 主机A     │  ☑ 主机B    │  ☑ 主机C       │ │
│  └────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

### 5.2 创建任务

1. 点击左侧 `新建任务` 按钮
2. 任务立即创建，名称默认为"新建任务1"（自动去重）
3. 任务名称自动进入编辑状态，可直接修改
4. 按 `Enter` 或点击其他区域自动保存名称

**其他方式编辑任务名称：**
- 双击已有任务的名称，进入行内编辑
- 右侧任务信息区也有名称输入框和描述输入框

### 5.3 任务列表操作

每个任务项右侧有三个图标按钮：

| 按钮 | 功能 |
|------|------|
| 🚀   | 立即执行该任务（切换到执行监控页开始执行）|
| 📋   | 复制该任务（深拷贝所有步骤，在新任务名称后加"副本"）|
| 🗑️   | 删除该任务（需确认）|

### 5.4 步骤类型

每个任务由多个有序步骤组成，支持两种步骤类型：

| 步骤类型 | 说明 |
|---------|------|
| 执行命令 | 在远程主机上执行 Shell 命令 |
| 传输文件 | 在本地和远程主机之间上传或下载文件 |

### 5.5 添加步骤

点击步骤画布底部的 `添加步骤` 按钮，弹窗配置：

#### 执行命令步骤

- **步骤名称**：自定义步骤名称（如"检查磁盘空间"）
- **命令**：要执行的 Shell 命令（如 `df -h`）
- **超时（秒）**：命令执行超时时间，默认 30 秒
- **执行策略**：勾选"步骤执行失败后，继续执行下一步"，则即使该步骤失败，后续步骤仍会继续执行

#### 传输文件步骤

- **步骤名称**：自定义步骤名称
- **传输方向**：
  - `上传（本地 → 远程）`：将本地文件上传到远程主机
  - `下载（远程 → 本地）`：从远程主机下载文件到本地
- **本地路径**：点击 `浏览` 按钮选择文件或目录
  - 上传时：选择要上传的本地文件
  - 下载时：选择保存目录，远程文件名会自动拼接
- **远程路径**：手动输入远程路径
  - 上传时：远程目标路径（支持自动创建目录）
  - 下载时：要下载的远程文件路径

> 💡 路径中的反斜杠（`\`）会自动转换为正斜杠（`/`），兼容 Windows 和 Linux。

### 5.6 步骤编辑弹窗中的执行策略

每个步骤可以独立设置 `continue_on_error` 策略：

- **未勾选**（默认）：步骤执行失败则停止整个任务
- **勾选**：步骤执行失败后仍继续执行后续步骤（适用于"尽量完成"的场景）

> 💡 勾选后，该步骤失败不会阻断任务，但失败结果仍会在执行监控中标记。

### 5.7 步骤卡片操作

每个步骤以卡片形式展示，卡片上有以下操作按钮：

| 按钮 | 功能 |
|------|------|
| ✏️ 编辑 | 打开步骤编辑弹窗，修改步骤配置 |
| 📋 复制 | 复制该步骤，在原步骤下方插入副本 |
| 📌 模板 | 将当前步骤保存为模板，方便复用 |
| ▲ 上移 | 将该步骤上移一位（改变执行顺序）|
| ▼ 下移 | 将该步骤下移一位 |
| ✕ 删除 | 删除该步骤（需确认）|

### 5.8 步骤模板功能

对于常用的步骤配置，可以保存为模板复用：

1. 点击步骤卡片上的 `📌 模板` 按钮
2. 输入模板名称，点击保存
3. 以后添加步骤时，点击 `📦 从模板添加` 快速填入配置

**模板管理：**
- 点击步骤画布底部的 `📋 模板管理` 按钮
- 在弹窗中可以查看、重命名、删除已保存的模板

### 5.9 选择目标主机

任务编辑完成后，需要在底部 `目标主机` 面板中选择要在哪些主机上执行：

1. 在搜索框中输入关键词筛选主机
2. 勾选要执行的主机
3. `全选` / `反选` 按钮可快速操作

> ⚠️ 未选中任何主机时，无法启动任务执行。

### 5.10 校验步骤

在以下时机系统会自动校验步骤配置：

- 添加步骤时
- 编辑步骤保存时
- 点击 `保存` 按钮时
- 启动任务执行时

**校验规则：**
- 步骤名称不能为空
- 执行命令步骤：命令不能为空
- 传输文件步骤：本地路径和远程路径均不能为空

校验不通过时会弹窗提示，并阻止相应操作。

---

## 6. 执行监控页

执行监控页用于启动任务执行、实时查看执行日志和进度。

### 6.1 启动任务执行

有两种方式进入执行监控：

1. **从任务编排页**：点击任务项的 `🚀` 按钮，自动切换页面并开始执行
2. **直接切换到执行监控页**：手动选择任务和主机后点击 `开始执行`

执行开始后，界面显示如下：

```
┌──────────────────────────────────────────────────────┐
│  步骤1: 检查磁盘空间     ████████████░░  80%        │ ← 当前步骤信息
├──────────────────────────────────────────────────────┤
│  主机1 (10.0.0.1)  ████████████████████ 100%  ✅  │ ← 各主机进度
│  主机2 (10.0.0.2)  ████████████░░░░░░  60%  🔄  │
│  主机3 (10.0.0.3)  ░░░░░░░░░░░░░░░░   0%  ⏳  │
├──────────────────────────────────────────────────────┤
│  总计：3台 | 成功：1 失败：0 执行中：1 待执行：1    │ ← 统计信息
│  [暂停/继续] [取消] [返回编辑]                      │
├──────────────────────────────────────────────────────┤
│  日志區                                      [主机▼]│
│  [10:30:01] [系统] 任务开始执行...                  │
│  [10:30:01] [web01] 步骤1开始执行...               │
│  [10:30:02] [web01] /dev/sda1  50G  20G  30G ... │
│  ...                                               │
└──────────────────────────────────────────────────────┘
```

### 6.2 步骤进度条

每个主机的进度条为**分段进度条**，每段代表一个步骤：

- **灰色段** — 待执行
- **蓝色段** — 执行中
- **绿色段** — 执行成功
- **红色段** — 执行失败

将鼠标悬浮在分段进度条上，会显示该步骤的名称和状态。

### 6.3 执行控制

| 按钮 | 功能 |
|------|------|
| 暂停/继续 | 暂停任务执行，或继续执行已暂停的任务 |
| 取消 | 取消任务执行（需确认）|
| 返回编辑 | 执行完成后返回任务编排页继续编辑任务 |

> 💡 任务执行过程中，状态栏左侧会显示任务名称和总耗时（格式：MM:SS）。

### 6.4 日志查看

执行监控页底部为日志区域，功能如下：

- **实时日志**：执行过程中实时输出命令结果和传输进度
- **主机筛选**：通过下拉框筛选只查看某台主机的日志（系统日志始终显示）
- **搜索高亮**：在日志区搜索关键词，匹配内容高亮显示
- **传输进度**：文件传输时以进度条形式显示，不在日志中刷屏；传输完成后输出一条摘要日志

**日志格式说明：**
```
[时:分:秒] [主机名] 日志内容
```

- `[系统]` 开头的日志行为系统日志（任务开始/结束、连接信息等）
- 常规日志前缀为对应的主机名

### 6.5 主机右键菜单

在执行监控页的主机列表中，右键点击某台主机，可弹出操作菜单：

- **重试该主机**：从该主机的第一个步骤重新开始执行
- **重试失败步骤**：从该主机第一个失败步骤开始重新执行（仅在有失败步骤时显示）

> 💡 总进度条上也支持右键，选择"重试所有主机任务"。

### 6.6 执行完成

任务执行完成后：

- 进度条根据最终结果变色（全部成功=绿色，有失败=红色）
- 状态栏显示最终结果
- 日志中输出执行摘要
- 可以点击 `返回编辑` 回到任务编排页修改任务

---

## 7. 关于对话框

点击菜单栏 `帮助` → `关于`，可查看软件信息：

```
SSH Remote Tool v0.1.0

SSH 远程运维工具
支持批量主机管理、文件传输和命令执行

作者：fan JC
邮箱：1071295829@qq.com
```

---

## 8. 常见问题

### Q1：软件的数据文件存在哪里？

数据库文件存储在：
```
C:\Users\你的用户名\.ssh-remote-tool\ssh_manager.db
```

日志文件存储在：
```
C:\Users\你的用户名\.ssh-remote-tool\logs\
```

可通过环境变量 `SRT_DB_DIR` 自定义数据目录。

### Q2：导入 CSV 后密码没有更新？

检查 CSV 文件中密码列是否为 `********`。该占位符表示"保持原密码不变"，将密码列改为真实密码后重新导入即可。

### Q3：步骤执行失败，但后续步骤还在执行？

检查步骤的"执行策略"是否勾选了"步骤执行失败后，继续执行下一步"。如需步骤失败即停止，取消勾选该选项。

### Q4：文件传输路径有什么注意事项？

- 远程路径请使用正斜杠（`/`），如 `/home/user/file.txt`
- Windows 本地路径可以使用反斜杠，软件会自动处理
- 上传时远程目录不存在会自动创建

### Q5：如何在多主机并行执行时查看单台主机的日志？

在执行监控页的日志区域右上角，有一个主机筛选下拉框，选择要查看的主机即可。系统日志始终显示，不受筛选影响。

### Q6：打包后的程序如何分发？

1. 运行 `build.bat` 生成打包文件
2. 打包产物在 `dist/SSHRemoteTool/` 目录
3. 如需安装包，使用 Inno Setup 编译 `installer.iss`，生成带中文向导的安装程序

---

*使用说明书生成日期：2026-04-30*
